# See language/examples/aes256.nrl for the canonical header and documentation.
# This copy exists so installs that ship ./examples/ can run the same benchmark.
# For FNV digest verification use examples/aes256_locked.nrl (requires current nrl).

mode=bench
profile=aes256-synth
neurons=4096
iterations=64
reps=1
threshold=6
